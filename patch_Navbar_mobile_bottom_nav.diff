<div className="lg:hidden w-full pt-2 border-t border-gray-200">
          <div className="flex items-center justify-between px-2">
            {/* User icons */}
            <UserHeader />
          </div>
          {/* Mobile nav links with small text */}
          <div className="flex justify-around mt-2 text-xs text-gray-700">
            <Link
              to="/"
              className={`flex flex-col items-center space-y-1 hover:text-amber-600 ${
                location.pathname === '/' ? 'text-amber-600' : ''
              }`}
            >
              <HomeIcon className="w-5 h-5" />
              <span>Beranda</span>
            </Link>
            {navLinks
              .filter((link) => link.name !== 'Beranda')
              .map((link) => (
                <Link
                  key={link.path}
                  to={link.path}
                  className={`flex items-center justify-center ${
                    location.pathname === link.path ? 'text-amber-600' : ''
                  }`}
                >
                  {link.name}
                </Link>
              ))}
          </div>
        </div>
=======
        {/* Mobile Custom Header */}
        <div className="lg:hidden w-full pt-2 border-t border-gray-200">
          <div className="flex items-center justify-between px-2">
            {/* User icons */}
            <UserHeader />
          </div>
        </div>
>>>>>>> REPLACE

<<<<<<< SEARCH
      </div>
    </nav>
  );
};
=======
      </div>
    </nav>

    {/* Mobile Bottom Navigation Bar */}
    <nav className="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 shadow-inner lg:hidden">
      <div className="container mx-auto px-4">
        <div className="flex justify-around text-xs text-gray-700">
          {navLinks.map((link) => (
            <Link
              key={link.path}
              to={link.path}
              className={`flex flex-col items-center justify-center py-2 transition-colors hover:text-amber-600 ${
                location.pathname === link.path ? 'text-amber-600' : ''
              }`}
            >
              {link.name === 'Beranda' ? <HomeIcon className="w-5 h-5 mb-1" /> : null}
              <span>{link.name}</span>
            </Link>
          ))}
        </div>
      </div>
    </nav>
  );
};
